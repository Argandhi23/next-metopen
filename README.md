# Next.js E-commerce (SSR)

Aplikasi e-commerce berbasis Next.js 15 (App Router) dengan implementasi Server-Side Rendering (SSR) penuh untuk penelitian performa web (Core Web Vitals & SEO).

## Menjalankan Aplikasi
1. `npm install`
2. `npm run dev`

Aplikasi akan berjalan di http://localhost:3000
